HTML5新特性：
1.新的Doctype
2.figure元素，用<figure>元素标记文档中的一个图像,<figcaption>元素为<figure>元素定义标题，应该放在<figure>中第一或最后一个元素的位置。
3.重定义<small>标签，标签定义小型文本（和旁注）
4.去掉link和script的type属性
5.contenteditable属性，让内容可编辑
6.新增表单类型
7.表单属性placeholder,required(必填),autofocus(自动获取焦点)
8.Web存储，sessionstorage,localstorage
9.语义化标签
10.Audio,Video
11.data-* 属性，自定义属性，js中可通过 element.dataset.* 获取属性，jQuery可以通过data()
12.Canvas
13.HTML5 拖放
14.地理定位
15.HTML5 Websocket

HTML语义化标签的好处：
HTML结构清晰，代码可读性更好，搜索引擎可以根据标签确定上下文和权重（SEO优化），便于团队开发维护

轮播图最后一页怎么处理：

HTML5的meta标签：

HTML5禁止苹果系统识别数字为手机号码：

image和canvas在处理图片的时候有什么区别：
image是通过对象的形式描述图片
canvas是通过专门的API将图片绘制在画布上

webSocket：WebSocket是Web应用程序的传输协议，它提供了双向的，按序到达的数据流。他是一个Html5协议，WebSocket的连接是持久的，他通过在
客户端和服务器之间保持双工连接，服务器的更新可以被及时推送给客户端，而不需要客户端以一定时间间隔去轮询。

HTTP和HTTPS的区别：
HTTP协议通常承载于TCP协议之上，在HTTP和TCP之间添加一个安全协议层（SSL或TSL），这个时候，就成了我们常说的HTTPS。
默认HTTP的端口号为80，HTTPS的端口号为443
HTTPS比HTTP协议安全

为什么HTTPS安全：https之所以比http安全，是因为他利用ssl/tls协议传输。它包含证书，卸载，流量转发，负载均衡，页面适配，浏览器适配，refer
传递等。保障了传输过程的安全性





